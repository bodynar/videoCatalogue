doctype html

h3 video-list

.videos-container
    app-video-filters.videos-filters
    .videos-list
        app-video-item.videos-item(
            *ngFor="let 'video' of (videos$ | async)",
            [video]='video'
        )